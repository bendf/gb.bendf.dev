{
  "name": "gb.bendf.dev",
  "version": "1.0.0",
  "description": "gb.bendf.dev site",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./dist && mkdir -p ./dist && cp -rL ./src/* ./dist/",
    "dev": "npx http-server ./src/ --port=8080",
    "fest": "npx playwright test",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0",
    "wrangler": "^4.75.0"
  }
}
